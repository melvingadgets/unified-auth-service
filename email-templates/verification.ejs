<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verify your Melvin Gadgets Account</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      background-color: #ffffff;
      color: #111111;
      margin: 0;
      padding: 0;
      line-height: 1.6;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 40px 20px;
    }
    .logo {
      font-size: 24px;
      font-weight: bold;
      letter-spacing: -0.5px;
      margin-bottom: 40px;
      color: #000000;
    }
    .greeting {
      font-size: 20px;
      font-weight: 600;
      margin-bottom: 16px;
    }
    .message {
      font-size: 16px;
      color: #444444;
      margin-bottom: 32px;
    }
    .button {
      display: inline-block;
      background-color: #000000;
      color: #ffffff !important;
      font-weight: 500;
      font-size: 16px;
      text-decoration: none;
      padding: 14px 28px;
      border-radius: 6px;
      margin-bottom: 32px;
    }
    .footer {
      font-size: 14px;
      color: #888888;
      border-top: 1px solid #eeeeee;
      padding-top: 24px;
    }
    .link-fallback {
      word-break: break-all;
      color: #666666;
      font-size: 14px;
      margin-top: 16px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">Melvin Gadgets</div>
    
    <div class="greeting">Hello <%= fullName %>,</div>
    
    <div class="message">
      Thank you for setting up your Melvin Gadgets account. Please verify your email address to get access to all our services.
    </div>
    
    <a href="<%= verifyUrl %>" class="button">Verify Email Address</a>
    
    <div class="footer">
      <p>If you didn't create an account, you can safely ignore this email.</p>
      
      <div class="link-fallback">
        Or copy and paste this link into your browser:<br>
        <a href="<%= verifyUrl %>" style="color: #666666;"><%= verifyUrl %></a>
      </div>
    </div>
  </div>
</body>
</html>
